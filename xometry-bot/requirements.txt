# Runtime deps for the Hetzner worker + review API.
# After install: `playwright install chromium` (buyer pricing + form submit).
httpx>=0.27
pydantic>=2.7
fastapi>=0.115
uvicorn>=0.30
psycopg[binary]>=3.2
playwright>=1.45
